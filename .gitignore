node_modules/

# Local permission-mode config (user/project overrides, never committed)
permission-mode.json
sandbox.json
.pi/

# Misc
*.local.json
*.log
.DS_Store

# Demo videos: the gallery video is hosted as a GitHub release asset
# (package.json pi.video), not in the repo
*.mp4
